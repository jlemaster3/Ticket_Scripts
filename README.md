Tickets are currently built as scripts using the /ToolBox_ECS_V1/ module that is in development, some older and archive tickets may use teh moduels and previous versions found under the '/obsolete/' directory.

The ‘/ToolBox_ECS_V1/’ directory is for a work in progress single import for a set of tools and actions to make large scale schanges to multipe IWS Jil files at the same time while loggs changes and saving output files to a new location. 

‘/ToolBox_ECS_V1/’ is still a Work in Progress.


Requirments:
    Pandas    
    Numpy
    Openpyxl